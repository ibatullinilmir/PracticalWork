<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Дано натуральное n (n >= 3). Получить Un" Height="250" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="n:"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtN" Margin="0,0,-275,0"/>
        <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="Вычислить" Click="Button_Click"/>
        <Label Grid.Row="2" Grid.Column="0" Content="Результат:"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtResult" IsReadOnly="True" Margin="0,0,-275,0"/>
    </Grid>
</Window>
